import {Typography} from "@mui/material";
import React from "react";

function Loading() {
  return (
    <Typography variant="h5" data-testid="loading-id">
      Loading...
    </Typography>
  );
}

export default Loading;
